package week4Project;

public class Snippet {
	for (String element : namesArray) { 
	  	 
	  	 ///Add the length of each string name to the total character count 
	  	        totalCharacterCount += element.length(); 
	  	        
}

